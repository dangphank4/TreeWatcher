import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors{
  AppColors._();

  static const Color primaryHighlight = Color.fromARGB(255, 0, 115, 255);
  static const Color primaryText = Colors.black;
  static const Color secondaryText = Color.from(
    alpha: 0.7,
    red: 1,
    green: 1,
    blue: 1,
  );
}